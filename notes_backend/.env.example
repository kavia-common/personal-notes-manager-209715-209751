# Example environment for notes_backend
# No secrets are required for local dev beyond Django SECRET_KEY already in settings for dev.
# To override port when running, use runserver argument: 0.0.0.0:3001
